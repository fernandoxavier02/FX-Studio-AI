{
  "SessionStart": [
    {
      "matcher": "",
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Pipeline Orchestrator v2.0 loaded. Single command: /pipeline [task]. Auto-classifies, confirms, executes in adaptive batches with TDD and per-batch adversarial review. Modes: /pipeline [task] | /pipeline diagnostic [task] | /pipeline continue | /pipeline --complexa [task]"
        }
      ]
    }
  ]
}
